# PREPARED — do not install until production promotion is approved.
# Drop-in candidate for wathefni-orchestrator.service (or EnvironmentFile):
WATHEFNI_APPLY_WHATSAPP_NUMBER=96599338566
WATHEFNI_FILE_POSITIONS_ENABLED=false
