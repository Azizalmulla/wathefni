# Unified Candidates staging screenshots

Captured 2026-07-25 against staging (`127.0.0.1:8011`) via SSH tunnel `:18011`, using a WATHEFNI owner dashboard session and temporary `UCSHOT*` fixtures (cleaned after capture).

| File | Proof |
| --- | --- |
| `unified-candidates-table.png` | All view; processing-attention banner → Intake Operations; held + live rows; Job `Not linked` / Status `Talent Pool` on held |
| `unified-candidates-filters.png` | Talent Pool view selected; 2 held rows; Job Not linked |
| `held-governed-profile.png` | Held profile drawer: sender provenance, missing-fact wording, Link to Job disabled, fact review append-only copy |
| `intake-operations.png` | Read-only Intake Operations surface |
| `staging-api-proof.json` | Redacted API samples for talent-pool / intake |
| `qualification.json` | Host evidence copy of the 50/50 staging qualify run |

Temporary fixtures cleaned (`UCSHOT*` apps/candidates = 0).
