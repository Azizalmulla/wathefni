# R5B historical reconstruction proof

Current org / policy changes must not rewrite old Performance truth.

| Artifact | How it is frozen | Staging proof |
|---|---|---|
| Objective / KR versions | C1 history rows | `objective history present`, `objective KRs reconstructable` |
| Cycle snapshot | Launch writes `snapshot_frozen` participant / form / config | `cycle snapshot frozen` |
| Reviewer identity / role at launch | Snapshot + assignment rows | Launch then `launched cycle immutable` |
| Review submissions | Distinct layer rows (self / manager / 360) | `layers not collapsed`; preserved after close |
| Calibration | Pre-cal vs calibrated vs sealed columns | Session detail keeps pre-cal after adjust/lock path |
| Final sealed result | `close_cycle` / lock; not a mutable rating | `final exists after close`; `self still distinct after close` |
| Competency framework version | C3 versioned framework / mappings | Competencies optional OFF still leaves Performance intact; ON uses stored version |
| Development state | C3 plan / action status + row version | Learning-off create + employee view; learning completion ≠ development completion |
| Module-off history | Rows retained; writes gated | `history preserved after disable` |

Post-launch `attempt_mutate_launched_setup` is rejected. HTTP cycle configure after launch cannot silently rewrite the snapshot.
