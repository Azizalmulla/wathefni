# Public, non-secret runtime config only. Anything prefixed EXPO_PUBLIC_ is
# embedded in the client bundle, so NEVER put secrets here.
EXPO_PUBLIC_API_BASE_URL=https://staging-api.wathefni.ai
# Development/staging only until production push delivery is approved.
EXPO_PUBLIC_PUSH_REGISTRATION_ENABLED=1
